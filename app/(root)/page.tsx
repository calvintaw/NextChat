import ContactPanel from "../ui/contact/ContactsPanel";

const Page = async () => {
	return (
		<section className="flex gap-2 flex-col flex-1">
			<ContactPanel></ContactPanel>
		</section>
	);
};

export default Page;
